# Frosty — DeviceIdle Patches
# Apps listed here will be patched for optimization (soft-removed from deviceidle whitelist).
# Add package names one per line. Lines starting with # are comments.

# com.android.vending